# Walkthrough - UI Refinement & Interaction Optimization

사용자께서 추가로 피드백 주신 (1) LinkedIn 아티클 썸네일 이미지 짤림 해결, (2) Case B 모달 스크롤 포커스, (3) Ecosystem Map 행열 바꿈, (4) 아웃링크 아이콘 정비, (5) 스킬 변경/스크롤 시 상세 내용 영역 펄스 강조 효과, (6) scrollIntoView 모달 하단 빈 여백 UI 밀림 버그 수정, 그리고 (7) 왼쪽 스킬 셀렉터 리스트의 자동 스크롤 포커스 연동 작업을 성공적으로 완료하였습니다.

---

## 작업 완료 목록

### 1. LinkedIn 아티클 모달 썸네일 16:9 비율 크롭 (Part 4, 5)
- **문제 원인**: 이전 턴에서 외곽 회색 영역을 완전히 없애려다 보니 크롭된 이미지 비율(1.88)이 브라우저의 16:9 (`aspect-ratio: 16 / 9`) 프레임보다 가로로 과하게 길어졌습니다. 이로 인해 브라우저가 `object-fit: cover`로 렌더링하면서 가로 좌우를 잘라내어 주요 텍스트가 잘려나갔습니다.
- **해결 방안**: 이미지 해상도를 **정확한 16:9 비율**인 `1690 x 951` 로 자르는 크롭 좌표 `(6, 0, 1696, 951)`를 적용했습니다. 
- **결과**: 좌우가 단 6px씩만 잘려나가 텍스트 영역을 안전하게 보존하며, 비율이 16:9에 완전히 수렴하므로 브라우저 렌더링 시 추가적인 좌우 짤림이 전혀 일어나지 않고 가득 채워져 노출됩니다.

### 2. Case B 모달 진입 시 스크롤 포커스 이동
- `CaseStudyModal.tsx`에 `explorerRef`를 추가하고, `useEffect` 및 `setTimeout(150ms)` 훅을 이용해 모달이 켜지고 렌더링이 안정화될 때 `initialSkillId`를 바탕으로 `skills-explorer-section` 엘리먼트로 부드러운 스크롤(`behavior: "smooth"`) 이동이 실행되도록 구현하였습니다.
- 메인 페이지의 Ecosystem Map에서 특정 스킬을 클릭하여 진입하는 즉시 탐색기 영역이 화면 최상단에 포커스되어 보입니다.

### 3. AI Skill Ecosystem Map 행열 바꿈 (Transpose)
- `CaseStudyCards.tsx`에 정의된 `AiSkillsEcosystemMap` 컴포넌트가 `isColumnLayout` prop을 지원하도록 수정하였습니다.
- 모달 내 하단 맵에 이를 활성화하여 기존 가로 행 방식에서 **상단에 Intake ~ Release 까지 5개의 가로 헤더(열)가 있고 그 아래로 각 스킬들이 세로로 배열되는 Transpose된 레이아웃**으로 변경하였습니다.
- 모달 내부에서는 Column Layout으로 보기 좋고 직관적으로 표시되며, 카드 외부 프리뷰는 좁은 가로 폭에 맞추어 기존 레이아웃이 유지됩니다.

### 4. 아웃링크 아이콘 Lucide ExternalLink 대체 및 컬러 매칭
- `src/App.tsx` 내의 clickable pillar 카드 영역에서 `↗` 텍스트 화살표 기호를 제거하고, `<ExternalLink size={14} className="outlink-icon" />` 아이콘 컴포넌트로 대체하였습니다.
- `styles.css`에 기 정의된 `.outlink-icon` 스타일을 통해 평소에는 Contact 섹션의 아이콘들과 일관된 컬러를 유지하며, 호버 시에는 Teal 컬러와 함께 위쪽 대각선으로 미세하게 움직이는 프리미엄 모션을 제공합니다.

### 5. 스킬 변경/스크롤 시 상세 내용 영역 펄스 강조 효과 적용
- **하이라이트 구현**: GNB Navigation 영역의 pulse 효과를 벤치마킹하여 `.skill-details-panel.pulse-highlight` 스타일을 CSS에 추가했습니다. 테두리가 영롱한 `var(--teal)` 색상으로 숨쉬듯 번쩍이면서 외부/내부 네온 글로우가 피어올랐다 사라지는 고급 애니메이션입니다.
- **인터랙션 연동**:
  - **스크롤 동반 시 (메인/모달 생태계 맵 스킬 클릭)**: 상단으로 부드럽게 스크롤링이 완료되는 시점인 **600ms~750ms 뒤**에 펄스 애니메이션이 정밀하게 동기화되어 발동합니다.
  - **스크롤 비동반 시 (좌측 탭 직접 클릭)**: 즉각적인 시각 피드백을 주기 위해 **0ms 딜레이(즉시)**로 펄스 애니메이션을 발동합니다.

### 6. scrollIntoView 모달 하단 빈 여백 UI 밀림 버그 수정
- **원인 분석**: 브라우저의 기본 `Element.scrollIntoView()` API는 조상 스크롤 가능한 요소들 중 브라우저 창(`window` / `body`)까지 함께 위로 이동시킵니다. 이로 인해 모달 전체 패널의 위치가 위로 쓸려 올라가며 모달 하단과 브라우저 하단 사이에 붕 뜨는 검은색 빈 영역이 나타나고 스크롤바가 생겼던 것입니다.
- **해결 방안**: 모달의 스크롤 영역에 `scrollContentRef`를 지정하고, 자식 요소의 상대 top 위치(`relativeTop = targetRect.top - containerRect.top + container.scrollTop`)를 정밀하게 계산해 **전체 윈도우 스크롤에 전혀 간섭이 없도록 모달 내부 컨테이너만 `container.scrollTo`를 사용해 부드럽게 스크롤**하도록 교체했습니다.
- **결과**: 자동 스크롤 작동 후에도 모달 하단이나 페이지 배경 레이아웃의 비정상적인 여백이 생기지 않고, 홈 화면 중앙에 모달창이 완벽하게 고정되어 렌더링됩니다.

### 7. 왼쪽 스킬 셀렉터 리스트의 자동 스크롤 포커스 연동
- **원인 분석**: 왼쪽의 10종 스킬 목록은 내부적으로 세로 스크롤 영역(`.skills-selector-list`)을 가집니다. 메인 화면이나 하단 지도로 아래쪽 스킬(예: `caid-jira`)을 클릭해 진입했을 때, 왼쪽 리스트 영역 자체는 스크롤되지 않아 활성화된 스킬 탭 버튼이 화면 밖(아래쪽)에 가려지는 현상이 있었습니다.
- **해결 방안**: 왼쪽 스킬 리스트 컨테이너에 `selectorListRef`를 할당하고, `activeSkillIdx`가 변경될 때마다 활성화된 버튼의 위치를 계산하는 `useEffect`를 작성했습니다. 만약 활성화된 스킬 버튼이 리스트 뷰포트 내에 보이지 않는 상태라면, **그 버튼이 왼쪽 스킬 리스트 영역의 세로 한가운데에 오도록 컨테이너만 정밀하게 `listContainer.scrollTo` 하도록 구현**했습니다.
- **결과**: 어떤 방식으로 스킬이 선택되어 전환되든 상관없이, 왼쪽 10종 스킬 리스트에서 활성화된 탭 버튼이 스크롤과 함께 항상 보기 좋은 정중앙에 자동으로 나타납니다.

---

## 화면 검증 결과 (Screenshots)

로컬 Vite 개발 서버(`http://127.0.0.1:5173/`)를 띄워 크롬 devtools-mcp를 통해 렌더링 상태를 확인하였습니다.

- **LinkedIn 아티클 모달 썸네일 (Part 4, 5) 텍스트 복구 검증**:
  - `Part 4` 및 `Part 5` 슬라이드 이미지 내부의 좌측 텍스트가 시작 알파벳("From", "What")부터 아주 선명하게 전체 문장이 다 표시되고 있습니다.
- **Case B 모달 및 에코시스템 맵 검증**:
  - 메인 화면에서 스킬 노드 클릭 시 모달이 켜지며 "구축된 AI 스킬 탐색기" 섹션으로 스크롤이 매끄럽게 포커싱됩니다.
  - 하단의 AI Skill 생태계 지도가 5열 Column Layout으로 깔끔하게 Transpose되어 렌더링 중입니다.
- **스킬 상세 카드 펄스 하이라이트 검증**:
  - 스택 맵 클릭으로 스크롤 이동이 완료된 후, 상세 카드 패널의 테두리가 네온 글로우 피드백과 함께 2초 동안 매끄럽게 펄스 하이라이트 되는 상태를 캡처 완료했습니다.
- **모달 하단 UI 밀림 버그 수정 검증**:
  - 자동 스크롤 완료 후에도 모달이 원래의 고정된 중앙 레이아웃을 고스란히 유지하며, 아래쪽에 불필요하게 붕 뜨던 검정 빈 여백이 완전히 사라진 깔끔한 상태를 캡처 완료했습니다.
- **왼쪽 스킬 셀렉터 리스트 자동 스크롤 검증**:
  - `caid-jira` 같이 아래쪽에 위치해 평소라면 가려졌을 스킬 탭 버튼이, 모달 진입 시 왼쪽 스크롤 리스트가 아래로 부드럽게 연동되어 정확히 뷰포트 정중앙에 포커싱된 채 노출되는 것을 확인 및 캡처 완료했습니다.
